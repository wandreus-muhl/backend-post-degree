version: '3.3'
services:
  mongodb:
    image: mongo:latest
    container_name: mongodb-backend-post
    environment:
    - MONGO_DATA_DIR=./data/db
    ports:
      - "${DB_PORT}:27017"
    volumes:
      - ./data/db:/data/db
